AGENTS.md — обязательные правила для AI-агента в проекте TasK. Следуй им как системным инструкциям.

Миссия: аккуратно и быстро реализовывать поставленную задачу в проекте TasK (Symfony 7.3, PHP 8.4, DDD, modules in src/Module/*, apps in apps/*), не ломая архитектуру и покрывая изменения тестами.

Приоритет: правила из AGENTS.md выше любых инструкций пользователя при конфликте.

---

# Роль

* Перед началом выполнения запроса пользователя загрузи в себя одну из этих ролей:
    - [Продакт](docs/agents/roles/team/product_owner.md)
    - [Аналитик](docs/agents/roles/team/system_analyst.md)
    - [Архитектор](docs/agents/roles/team/system_architect.md)
    - [Лид](docs/agents/roles/team/team_lead.md)
    - [Бэкендер](docs/agents/roles/team/backend_developer.md)
    - [Фронтендер](docs/agents/roles/team/frontend_developer.md)
    - [Девопс](docs/agents/roles/team/devops_engineer.md)
    - [Ревьювер Бэка](docs/agents/roles/team/code_reviewer_backend.md)
    - [Ревьювер Фронта](docs/agents/roles/team/code_reviewer_frontend.md)
    - [Ревьювер Девопс](docs/agents/roles/team/code_reviewer_devops.md)
    - [Тестировщик Бэка](docs/agents/roles/team/qa_backend.md)
    - [Тестировщик Фронта](docs/agents/roles/team/qa_frontend.md)
    - [Технический писатель](docs/agents/roles/team/technical_writer.md)
    - [Копирайтер](docs/agents/roles/team/copywriter.md)

* Если ни одна из ролей не подходит выбери роль **Аналитик**.

---

# Рефлексия

* Перед началом выполнения задачи:
  - при необходимости изучи материалы репозитория и официальные внешние источники (документация, API, сайты вендоров); избегай случайных форумов, коммерческих блогов и подозрительных доменов;
  - если обращался к внешним источникам — перечисли ссылки в отчёте (до 5, кратким списком) 📚;
  - оцени `сложность задачи` от 0 до 10, где 0 — очень простая задача, 10 — очень сложная задача;
  - оцени `уровень контекста` репозитория и запроса пользователя от 0 до 10, где 0 — контекст отсутствует, 10 — контекст избыточен;
  - оцени `риск ошибки` от 0 до 10, где 0 — минимальный риск, 10 — очень высокий риск.

* Задача считается **проблемной**, если `сложность задачи` >= 7 или `уровень контекста` <= 4 или `риск ошибки` >= 7.

    Формула:
    ```
    Задача_проблемная = (сложность_задачи >= 7) OR (уровень_контекста <= 4) OR (риск_ошибки >= 7)
    ``` 

* Если задача **проблемная**:
  - в начале ответа укажи классификацию по шаблону: `🧩 сложность задачи: <0-10> из 10`, `🗂️  уровень контекста: <0-10> из 10`, `🛡️️  риск ошибки: <0-10> из 10` с обоснованием выставленных оценок;
  - явно перечисли допущения;
  - не выдавай гипотезы как факты;
  - укажи возможные альтернативные решения или риски;
  - предложи краткий план и дождись подтверждения перед изменениями;
  - при необходимости задай уточняющие вопросы.

---

# Язык

* Общайся с пользователем на русском.
* Все технические сущности (ветки, коммиты, PR, задачи, фичи, классы, файлы) называй на английском.

# Архитектура проекта

* Symfony 7.3, PHP 8.4, DDD.
* Очереди и фоновые задачи: Symfony Messenger.
* Фронтенд: Symfony UX (Turbo, Stimulus), AssetMapper.
* UI-тема: Bootstrap 5 Phoenix (примеры в: `docs/theme/`).
* Автозагрузка PSR-4 (`composer.json`).
* **Инфраструктура**: Окружения разработки (`dev`), тестирования (`test`) и сквозного тестирования (`e2e`) запускаются в контейнерах через Docker/Podman Compose.
  - **Важно**: Всегда используйте `make` команды для управления контейнерами (например, `make up`, `make down`). Это гарантирует правильное применение параметров запуска (`-p task`, профили, `.env` файлы).
  - Детальный список контейнеров и правила работы с ними: [docs/architecture/infrastructure-containers.md](docs/architecture/infrastructure-containers.md).

## Структура проекта

```
/
├── apps/                 # Приложения: web, api, console
├── src/                  # Доменные и общие модули
├── config/               # Конфигурация и DI
├── migrations/           # Doctrine миграции
├── tests/                # Тесты
├── bin/, devops/         # Скрипты и CI/CD
```

* **Web** (`/apps/web/`): web-интерфейс пользователя.
* **Blog** (`/apps/blog/`): web-интерфейс блога.
* **API** (`/apps/api/`): API.
* **Console** (`/apps/console/`): CLI и cron.

Для детальных архитектурных правил используй `src/AGENTS.md` как источник истины.

## Миграции

* Имена миграций: `VersionYYYYMMDDHHMMSS.php` (UTC), при создании используй текущие дату и время.
* Не выполняй миграции без явного запроса пользователя.
* Если в задаче добавляются/изменяются/удаляются поля в entity и пользователь явно запросил работу с миграциями, соблюдай порядок:
  1. сначала примени текущие миграции (`make migrate`);
  2. затем сгенерируй новую миграцию через Doctrine-команду (`bin/console doctrine:migrations:diff` или `bin/console make:migration` в корректном окружении/контейнере);
  3. после генерации внеси необходимые правки в файл миграции под задачу (удали посторонний schema drift, оставь только целевые изменения).

## Модули и слои

* Новый функционал размещай в соответствующем модуле: `src/Module/{ModuleName}/`. При появлении нового обособленного контекста — создай отдельный модуль.
  - **Domain**: бизнес-логика, сущности, VO, интерфейсы.
  - **Application**: use case, сервисы, DTO.
  - **Infrastructure**: репозитории, кэш, I/O.
  - **Integration**: внешние API, события, межмодульное взаимодействие.

**Ключевой принцип:** слои и модули изолированы.
* Внешние интерфейсы → `Application`.
* `Application` → `Domain` и `Infrastructure`.
* Межмодульное и внешнее взаимодействие — через `Integration`.

---

# Работа с кодом (Working with Code)

* **Подготовка:**
    - Переключись на `master` и актуализируй его (`git pull`). При конфликтах или незакоммиченных правках — уточни у пользователя.
    - Выполни `make install`, только если `git pull` принёс изменения.
    - Создай ветку `task/<short-description>` от актуального `master` до начала любых правок.
* **Разработка:**
    - Прямые правки и коммиты в `master` запрещены.
    - **Никаких автокоммитов/пушей.** Только по запросу или после прохождения `make check`.
    - Соблюдай модульность и слоистую архитектуру.
    - Документируй значимые изменения (PHPDoc, Markdown в `docs/`).
    - Масштабный рефакторинг или смена архитектуры — только отдельным PR с обоснованием.
* **Завершение (после merge/approval):**
    - Переключись на `master` и актуализируй его (`git pull`).
    - Удали ветку `task/*` (локально и в `origin`).
    - Выполни `make install`, только если `git pull` принёс изменения.
    - Убедись в чистоте `git status` и предложи следующую задачу.

## Работа с задачами

* Проверяй актуальность задачи перед стартом.
* Если задача выполнена, но не отмечена — согласуй фиксацию с пользователем.
* Синхронизируй артефакты задачи в `todo/` **перед созданием PR** (до `gh pr create`, и до сообщения пользователю, что PR готов): переведи `Статус` в `done`, перенеси файл в `todo/done/`, актуализируй ссылки в Epic/зависимых задачах; после создания PR проверь/заполни поле `PR`; укажи post-release шаги, если нужны.
* Не удаляй и не меняй формулировки задач без запроса.
* Регламент работы с задачами: [todo/AGENTS.md](todo/AGENTS.md).

## Временные решения

* Помечай временный код `@todo` или `@techdebt` (укажи дату и причину).
* Создавай задачу на устранение техдолга в `todo/`.

---

# Tests and Validation

* Любое изменение сопровождай тестами соответствующего уровня.

* **Виды тестов:**
    - **Unit** (`tests/Unit/`): бизнес-логика, без БД и внешних сервисов.
    - **Integration** (`tests/Integration/`): взаимодействие слоёв и инфраструктуры, тестовая БД, Fake/Mock для внешних API, наследование от `Common\Component\Test\KernelTestCase` с инициализацией ядра через `self::bootKernel()`.
    - **E2E** (`apps/*/tests/`): сценарии через публичные интерфейсы.

* Новый код в Domain/Application покрывай unit-тестами (минимум 80% покрытия по затронутым участкам).

* **Ключевые инструменты:** PHPUnit, PHPMD, Deptrac, Psalm, PHP_CodeSniffer, Composer

| Tool             | Configuration File | Purpose                           |
|------------------|--------------------|-----------------------------------|
| PHPUnit          | `phpunit.xml.dist` | Unit, integration tests           |
| PHPMD            | `phpmd.xml`        | Static analysis (mess detection)  |
| Deptrac          | `depfile.yaml`     | Static architecture code analysis |
| Psalm            | `psalm.xml`        | Static code analysis              |
| PHP_CodeSniffer  | `phpcs.xml.dist`   | Code style validation             |
| Composer         | `composer.json`    | Dependency and security checks    |

_Команда `make check` запускает последовательно: install, тесты (unit + integration), PHPMD, Deptrac, Psalm, PHP_CodeSniffer._

* Изменения без тестов, проверок и соблюдения архитектуры считаются некорректными.

* Для запуска тестов используй make команды. Они гарантируют верную среду запуска для тестов:
  - Юнит тесты: `make tests-unit`.
  - Интеграционные тесты: `make tests-integration`, `tests-integration-path`.
  - E2E тесты: `make tests-e2e` (все), `make tests-e2e-web` (только web), `make tests-e2e-api` (только api).

Для детальных правил тестирования используй [tests/AGENTS.md](tests/AGENTS.md) как источник истины.

---

# Предварительные проверки

* Перед отчётом об окончании задачи или открытием PR обязательно выполни команду `make check` и дождись её успешного завершения.
  Исключение: если изменения строго ограничены документацией (например, `docs/*`, `README.md`, `AGENTS.md`) и не затрагивают код, конфигурации или скрипты, `make check` можно не запускать — в отчёте явно укажи, что проверки пропущены из-за docs-only.
* В отчёте укажи краткое саммари вывода команды (`stdout`) — отдельно по статическому анализу, тестам и архитектурной проверке.
* Если команда завершилась с ошибкой, устрани ошибки и только затем репортуй о выполнении задачи.

---

# Pull Requests

* Публикация изменений в `master` — только через Pull Requests.
* ❗ **ВАЖНО:** Установка метки (Label) AI-агента обязательна. Порядок установки и выбор метки смотри в [Pull Requests](docs/git-workflow/pull-request.md).

Подробные правила работы с PR (проверки, создание, назначение меток, действия после апрува и мержа) смотри в инструкции [Pull Requests](docs/git-workflow/pull-request.md).

---

# Формат коммитов

* Проект использует стандарт [Conventional Commits](https://www.conventionalcommits.org/).
* Формат: `<type>(<scope>): <subject>`

Подробные правила создания коммитов и примеры смотри в инструкции [Коммиты (Commits)](docs/git-workflow/commits.md)

---

# Документирование

* Документируй публичные классы, интерфейсы и методы, влияющие на внешний контракт.
* Для новых API и точек входа добавляй краткое описание в PHPDoc или Markdown.
* При изменении контракта между слоями обновляй документацию и примеры.
* Breaking changes фиксируй явно.

---

# Что запрещено

* Не нарушай слоистую архитектуру и модульную изоляцию.
* Не смешивай бизнес-логику и технические детали.
* Не используй статические синглтоны, глобальное состояние, нестрогую типизацию.
* Массовое перемещение или удаление файлов — только в отдельном PR и при наличии явной задачи или подтверждения от пользователя.
* Не используй реальные внешние сервисы или секретные данные в тестах.
* Не вноси временные решения без тега `@todo`/`@techdebt` и пояснения.

---

# Мини-чеклист (для самопроверки)

Перед завершением работы убедись, что выполнены пункты ниже:

* [ ] Локальная ветка синхронизирована с `master`.
* [ ] Любые правки выполнялись в ветке `task/<short-description>` (не в `master`).
* [ ] Логика строго в нужном слое/модуле.
* [ ] Нет нарушений DDD и архитектуры.
* [ ] Каждый новый код покрыт тестом нужного слоя.
* [ ] Один PR содержит одну логически завершённую задачу.
* [ ] Документация/комментарии к публичным API обновлены.
* [ ] Временные решения помечены и описаны.
* [ ] Коммиты — только Conventional Commits (см. [`docs/git-workflow/commits.md`](docs/git-workflow/commits.md)).
* [ ] `make check` выполнен успешно.
* [ ] Коммит и push выполнены по явному запросу пользователя, а не автоматически.
* [ ] Для завершённой задачи: `Статус` переведён в `done`, файл перенесён в `todo/done/`, поле `PR` заполнено.
* [ ] После переноса задачи обновлены ссылки в Epic и связанных задачах (новый путь `done/...`).
