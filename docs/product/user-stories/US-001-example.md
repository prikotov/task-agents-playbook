# US-001: Загрузка PDF через CLI и поиск через API

**Status:** 📋 Planned

---

## Actor/Context

**Actor:** Разработчик (Developer)

**Context:** Разработчик хочет проиндексировать PDF-документ с технической документацией и использовать API для программного поиска по его содержимому.

---

## Goal

Разработчик хочет загружать PDF-файлы через CLI команду, чтобы система автоматически индексировала их, и затем мог выполнять semantic search через REST API.

---

## Value

- **Автоматизация:** Программный доступ к поиску позволяет интегрировать TasK в существующие рабочие процессы
- **Эффективность:** Разработчик может быстро находить нужную информацию в большом объёме документации
- **Повторяемость:** CLI интерфейс позволяет автоматизировать ingest в CI/CD или скриптах

---

## Story

**Как** разработчик,
**Я хочу** загружать PDF-документ через CLI команду,
**Чтобы** система автоматически индексировала его, и я мог искать по содержимому через API.

---

## Acceptance Criteria

### CLI Ingestion
- [ ] Команда `bin/console app:source:ingest <path>` принимает путь к файлу
- [ ] Команда возвращает уникальный ID источника (source_id)
- [ ] Команда запускает асинхронную обработку в очереди
- [ ] Команда проверяет, что файл существует и доступен для чтения
- [ ] Команда возвращает ошибку, если формат файла не поддерживается

### Processing
- [ ] Извлекается текст из PDF (используя pdfinfo/Docling)
- [ ] Текст разбивается на chunks (intelligent chunking)
- [ ] Генерируются embeddings для каждого chunk
- [ ] Вектора индексируются в PostgreSQL + pgvector
- [ ] Source status обновляется до "indexed"

### API Search
- [ ] API endpoint `POST /api/v1/search` принимает JSON с параметрами:
  ```json
  {
    "query": "string",
    "limit": 10,
    "offset": 0
  }
  ```
- [ ] API возвращает результаты в формате:
  ```json
  {
    "results": [
      {
        "chunk_id": "uuid",
        "content": "string",
        "score": 0.95,
        "source": {
          "id": "uuid",
          "filename": "document.pdf"
        }
      }
    ],
    "total": 100,
    "query": "string"
  }
  ```
- [ ] API выполняет semantic search по индексу (не keyword search)

### Error Handling
- [ ] При ошибке обработки source статус становится "failed"
- [ ] API возвращает 4xx для client errors и 5xx для server errors
- [ ] Error response содержит понятное сообщение

---

## Non-Goals

- ❌ Валидация прав доступа (MVP — internal use only)
- ❌ Поддержка других форматов кроме PDF (будет в следующих US)
- ❌ Фильтрация результатов по метаданным (будет в следующих US)
- ❌ Экспорт результатов в другие форматы (будет в следующих US)

---

## Notes/Open Questions

**Open Questions:**
- Какой максимальный размер файла для ingest? (по умолчанию: 50MB)
- Сколько chunks на документ? (adaptive chunking, ~500-1000 tokens)
- Какой LLM для embeddings? (по умолчанию: Ollama с nomic-embed-text)

**Notes:**
- Для MVP используется нестабильный API; может меняться без notice
- CLI команды запускаются в контейнере `worker-cli`
- API endpoint пока без аутентификации (MVP scope)

---

## Links

- **Related Stories:**
  - [US-002](US-002-example.md) — Product manager creates project and team searches via web
  - US-003 — User configures S3 bucket as source

- **Related Docs:**
  - [MVP](../mvp.md) — MVP scope and acceptance criteria
  - [Vision](../vision.md) — product vision
  - [Story Mapping](../story-mapping.md) — release planning

- **Architecture:**
  - [ADR-0001](../../architecture/adr/ADR-0001-example.md) — Vector DB selection (PostgreSQL + pgvector)
  - [ADR-0002](../../architecture/adr/ADR-0002-example.md) — Internal-first API policy
