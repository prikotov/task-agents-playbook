# 📘 Алгоритм постановки задачи LLM (адаптированный из курса и профиля Codex)

## 1. Определи исполнителя
- Выбор модели (Codex, GPT-4, Claude и др.).
- Уточнение сильных сторон модели:
  - Codex: сильный в Python, средней силы в PHP/Symfony, слабее в CSS/Bootstrap.
- Если модель ограничена, предусмотреть роль разработчика для доработки.

⚠️ Риск: если модель выбрана неверно, результат будет шумным или нерелевантным.

## 2. Дай контекст и цель
- Объясни, зачем нужна фича, какая бизнес-ценность за ней стоит.
- Укажи версию окружения (например, PHP 8.2, Symfony 6.3, Bootstrap 5.3, браузеры).

⚠️ Риск: без бизнес-контекста модель может решить «технически», но бесполезно.

## 3. Опиши образ результата (SMART)
- Формат (файл, код, тесты, PR).
- Сроки, ограничения, измеримость.
- Ссылки на документацию (AGENTS.md, API docs, Symfony docs).

⚠️ Риск: без формата модель выдаст длинный текст вперемешку с кодом.

## 4. Укажи шаги или рамки выполнения
- Ограничения по языку/библиотекам.
- Разбей на этапы (API → код → тесты → PR).

⚠️ Риск: модель «придумает» лишнее или уйдёт в сторону.

## 5. Согласуй критерии успеха
- Все тесты проходят, `make check` без ошибок.
- Conventional Commits соответствуют правилам.
- Покрытие тестами ≥80%.

⚠️ Риск: без критериев код будет непроверяемым.

## 6. Задай точки контроля (=итерации)
- Псевдокод → код → оптимизация.
- Проверка против документации.

⚠️ Риск: без итераций придётся переписывать целиком.

## 7. Проверка понимания
- Переформулировка задачи моделью.
- Согласование плана реализации.

⚠️ Риск: без проверки понимания модель может идти в неверном направлении.

## 8. Мотивация (=режим генерации)
- Skeleton mode (каркас) или Full implementation mode (рабочий код).
- Укажи стиль: минимализм, комментарии, PEP8.

⚠️ Риск: по умолчанию модель пишет в «своём» стиле.

---

## ⚠️ Частые ошибки постановки задач LLM
- Перегрузка контекста: слишком длинный ввод.  
- Отсутствие тестов и CI/CD критериев.  
- Двойные инструкции («сделай коротко, но подробно»).  
- Нет версии окружения.  

---

## 📈 Как развивается навык постановки задач
- **Начальный уровень**: формулировка результата.  
- **Средний уровень**: добавление критериев успеха и контрольных точек.  
- **Продвинутый уровень**: учёт CI/CD, версий, документации, режимов генерации.  

---

## 🛠 Пример применения
❌ Плохо: «Сделай функцию для проверки email».  

✅ Хорошо:  
«Реализуй функцию `is_valid_email(email: str) -> bool` в Python 3.11.  
Ограничения: только стандартная библиотека.  
Критерии: проходит тесты и `make check`.  
Сначала опиши алгоритм, потом напиши реализацию.  
Формат: только код.  

```python
assert is_valid_email("user@example.com") == True
assert is_valid_email("bad-email") == False
```

---

📚 Основано на курсе «Стратоплан» (фрагменты 28, 31–32, 45–46, 68, 96) и профиле Codex.
